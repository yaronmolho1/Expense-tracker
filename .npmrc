# Enable build scripts for native modules
enable-pre-post-scripts=true

# Approved build scripts
@bcrypt=bcrypt
@esbuild=esbuild
@sharp=sharp
@unrs=unrs-resolver

# Store configuration for better Docker layer caching
store-dir=~/.pnpm-store
